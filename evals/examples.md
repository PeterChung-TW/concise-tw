# 改寫前後範例（評測基準）

> 測法：把「開啟前」的回應餵給裝了本 skill 的 session，說「精簡」後同題重答，
> 對照是否符合十條。以下每組標註命中的規則編號。

## 例一：修 bug 回報（規則 1、5、6）

**開啟前**：
> 我仔細看了一下這個錯誤，這是一個蠻常見的問題。讓我先解釋一下背景：這個函式在處理空字串的時候……（300 字）……所以大概就是這樣的情況，我建議可以考慮修改一下判斷式。

**開啟後**：
> 要改的是 `parser.py:88`——`if val:` 改 `if val is not None:`，空字串被誤判成缺值。
> 已改並跑過 `pytest tests/test_parser.py`：12 passed。
> 下一步：無，靜待。

## 例二：多步驟任務（規則 2、3、4）

**開啟前**：
> 好的！這個部署流程有幾個部分需要處理，首先我們要先確認環境變數，然後可能還要看一下設定檔，接著就可以開始部署了，部署完成之後記得要驗證一下……

**開啟後**：
> 第 1 步／共 3 步：部署前檢查。
> 1. `.env` 缺 `API_KEY`——已從範本補上佔位，要填真值。
> 2. `config.yaml:12` 指向 staging，已改 production。
> 3. 部署指令準備好了，等你確認第 1 項後執行。
> 下一步（兩分鐘內）：把 `API_KEY` 真值填進 `.env`。

## 例三：時間估計（規則 7）

**開啟前**：
> 這個批次處理大約需要 5 到 10 分鐘就可以完成了。

**開啟後**：
> 這批 45 個檔案。上次跑 43 個花了 19.8 分鐘，這批量級相同。

## 例四：長清單（規則 8）

**開啟前**：
>（一則訊息列出 41 條待辦，要滑三個畫面才看得完）

**開啟後**：
> 41 條看完，要你決定的前 5 條：（5 條）
> 完整清單在 `待辦清單_2026-08-25.md`，其餘 36 條連同證據都在檔內。

## 例五：岔題（規則 9）

**開啟前**：
>（修 A 問題修到一半，發現 B 問題，當場岔去修 B，回報混在一起）

**開啟後**：
> A 已修好（證據）。另發現 B（`log.py:31` 的編碼假設），沒動，已記待辦。
> 下一步：確認 A 的修法後，決定 B 要不要開單。

## 例六：開場與收尾（規則 10）

**開啟前**：
> 好問題！這是一個很好的觀察……（中略）……希望這樣有幫助，還有什麼需要我做的嗎？

**開啟後**：
>（首句直接是結論）……
> 改了什麼：X。怎麼驗的：Y。你下一步：Z。
